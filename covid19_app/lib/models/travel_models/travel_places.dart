class TravelPlaces {
  String national;
  String city;
  String district;
  String address;
  DateTime time;
  TravelPlaces(
      this.national, this.city, this.district, this.address, this.time);
}
